rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // KDB exposes public reads and all owner writes through the Cloudflare
    // Worker. The browser must not receive an unrestricted Firestore path.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
