{
  "name": "kdb-firebase-functions",
  "private": true,
  "type": "module",
  "main": "index.mjs",
  "engines": { "node": "20" },
  "scripts": {
    "check": "node --check index.mjs",
    "test": "node --test",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
